{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'cd backend && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT'"
  }
}